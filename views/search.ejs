<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - Hymns</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-logo">
                <a href="/">
                    <img src="/assets/logo.svg" alt="Hymns Logo" class="logo">
                    <span>Hymns</span>
                </a>
            </div>
            
            <div class="nav-search">
                <form action="/search" method="GET">
                    <div class="search-box">
                        <input type="text" name="q" placeholder="Search hymns..." id="search-input" value="<%= query %>">
                        <button type="submit"><i class="fas fa-search"></i></button>
                    </div>
                </form>
            </div>
            
            <div class="nav-menu">
                <a href="/" class="nav-link">Home</a>
                <div class="nav-dropdown">
                    <a href="#" class="nav-link">Languages <i class="fas fa-chevron-down"></i></a>
                    <div class="dropdown-content">
                        <a href="/amharic/worship">Amharic</a>
                        <a href="/oromo/worship">Afan Oromo</a>
                        <a href="/tigrigna/worship">Tigrigna</a>
                        <a href="/english/worship">English</a>
                    </div>
                </div>
                <% if (user) { %>
                    <a href="/users/profile" class="nav-link">Profile</a>
                    <a href="/users/logout" class="nav-link">Logout</a>
                <% } else { %>
                    <a href="/users/login" class="nav-link">Login</a>
                    <a href="/users/register" class="nav-link btn-primary">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="search-results-container">
        <div class="container">
            <div class="search-header">
                <h1>Search Results for "<%= query %>"</h1>
                <p>Found <%= hymns.length %> hymns matching your search</p>
            </div>
            
            <% if (hymns.length > 0) { %>
                <div class="search-results">
                    <% hymns.forEach(function(hymn) { %>
                        <div class="search-result-item">
                            <div class="result-content">
                                <div class="result-image">
                                    <div class="language-badge">
                                        <%= hymn.hymnLanguage ? hymn.hymnLanguage.charAt(0).toUpperCase() + hymn.hymnLanguage.slice(1) : 'Unknown' %>
                                    </div>
                                    <i class="fas fa-music"></i>
                                </div>
                                
                                <div class="result-details">
                                    <h3><a href="/hymns/<%= hymn._id %>"><%= hymn.title %></a></h3>
                                    <p class="result-description"><%= hymn.description %></p>
                                    
                                    <div class="result-meta">
                                        <span class="category-tag">
                                            <%= hymn.category ? hymn.category.charAt(0).toUpperCase() + hymn.category.slice(1) : 'Unknown' %>
                                        </span>
                                        <span class="meta-item">
                                            <i class="fas fa-play"></i> <%= hymn.plays || 0 %> plays
                                        </span>
                                        <span class="meta-item">
                                            <i class="fas fa-star"></i> <%= hymn.rating ? hymn.rating.toFixed(1) : '0.0' %>
                                        </span>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="result-actions">
                                <a href="/hymns/<%= hymn._id %>" class="btn btn-primary btn-small">
                                    <i class="fas fa-play"></i> Listen
                                </a>
                                <% if (user) { %>
                                    <button class="btn-icon favorite-btn" data-hymn-id="<%= hymn._id %>">
                                        <i class="far fa-heart"></i>
                                    </button>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <i class="fas fa-search"></i>
                    <h3>No results found</h3>
                    <p>We couldn't find any hymns matching "<%= query %>". Try adjusting your search terms or browse by language and category.</p>
                    
                    <div class="empty-state-actions">
                        <a href="/" class="btn btn-primary">Back to Home</a>
                        <a href="/amharic/worship" class="btn btn-outline">Browse All Hymns</a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <div class="footer-logo">
                        <img src="/assets/logo-white.svg" alt="Hymns Logo">
                        <span>Hymns</span>
                    </div>
                    <p>Sharing the beauty of Ethiopian Orthodox hymns with the world.</p>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2023 Hymns Project. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>